# Database Configuration
POSTGRES_USER=maxillaris
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=maxillaris_db

# Node Environment
NODE_ENV=development

# Application Port (optional, default: 5000)
PORT=5000
